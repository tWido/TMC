
typedef struct{
    uint8_t *dest;
    uint8_t *source;
} parameters;

int CheckRestrictions(parameters to_check){
    //not implemented
    // check if parameters for device are within allowed range
    return 0;
}

